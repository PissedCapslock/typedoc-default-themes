{{#compact}}
    {{#if this}}
        {{#if elementType}}
            {{#with elementType}}
                {{> typeAndParent}}
            {{/with}}
            []
        {{else}}
            {{#if reflection}}
                {{#ifSignature reflection}}
                    {{#if reflection.parent.parent.url}}
                        <a href="{{relativeURL reflection.parent.parent.url}}">{{formatDisplayName reflection.parent.parent.name}}</a>
                    {{else}}
                        {{formatDisplayName reflection.parent.parent.name}}
                    {{/if}}
                    .
                    {{#if reflection.parent.url}}
                        <a href="{{relativeURL reflection.parent.url}}">{{formatDisplayName reflection.parent.name}}</a>
                    {{else}}
                        {{formatDisplayName reflection.parent.name}}
                    {{/if}}
                {{else}}
                    {{#if reflection.parent.url}}
                        <a href="{{relativeURL reflection.parent.url}}">{{formatDisplayName reflection.parent.name}}</a>
                    {{else}}
                        {{formatDisplayName reflection.parent.name}}
                    {{/if}}
                    .
                    {{#if reflection.url}}
                        <a href="{{relativeURL reflection.url}}">{{formatDisplayName reflection.name}}</a>
                    {{else}}
                        {{formatDisplayName reflection.name}}
                    {{/if}}
                {{/ifSignature}}
            {{else}}
                {{this}}
            {{/if}}
        {{/if}}
    {{else}}
        void
    {{/if}}
{{/compact}}